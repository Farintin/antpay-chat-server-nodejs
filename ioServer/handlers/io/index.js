/* const { 
  socketDisconnectHandler, 
  sendMessageSocketHandler,
  joinRoomsSocketHandler,
  // retrieveOldChatMessages,
  msgTrackClientPing2,
  msgTrackClientPing3
} = require('../socket') */
const {
  onConnectedSocket
} = require('../socket')





module.exports = {
    onConnectedSocket
}